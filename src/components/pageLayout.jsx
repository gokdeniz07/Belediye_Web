import React from 'react';

const PageLayout = ({ children }) => {
  return (
    <div className="w-full bg-gradient-to-br from-white via-gray-50 to-gray-100 min-h-screen pt-28 pb-8">
      <div className="w-full">
      
      </div>
    </div>
  );
};

export default PageLayout;
